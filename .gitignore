# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build

# attendance data — database, selfies, encryption key. NEVER commit this.
/data/

# local test-run state and reports
/test-run/

# The built Windows agent IS committed, so a deployment can hand it to employees.
# Rebuild with: npm run build:agent  (Windows only)
/agent/obj/
/agent/bin/*
!/agent/bin/PulseAgent.exe

# env files
.env*
!.env.example

# misc
.DS_Store
*.pem
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
/coverage
*.tsbuildinfo
next-env.d.ts
.vercel

# scratch scripts
/scripts/_*
